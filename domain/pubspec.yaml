publish_to: none
# This file is used to define the Dart package for the domain layer of the application.
name: domain
description: A sample command-line application.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.8.1

# Add regular dependencies here.
dependencies:
  #local packages
  common:
    path: ../common

  #  3rd party packages
  dartz: ^0.10.1
  dio: ^5.9.0
  freezed_annotation: ^3.1.0
  # path: ^1.8.0

dev_dependencies:
  build_runner: ^2.6.0
  freezed: ^3.2.0
  lints: ^5.0.0
  test: ^1.24.0
